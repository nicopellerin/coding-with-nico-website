// import fs from 'fs'
// import { join, resolve } from 'path'
// import matter from 'gray-matter'

// const fsPromises = fs.promises


export const getTipsSlugs = async () => {
//    const dirs = await fsPromises.readdir(directory, { withFileTypes: true })
//    console.log('DIRS-------', dirs)
//    const files: any = await Promise.all(dirs.map(dir => {
//        const res = resolve(directory, dir.name)
//        return dir.isDirectory() ? getTipsSlugs(res) : res
//    }))

//    return files
}