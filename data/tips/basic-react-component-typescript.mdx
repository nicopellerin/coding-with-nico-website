---
title: 'Basic React component with Typescript'
tech: 'typescript'
date: 1617644149294
ogImage:
  url: '/og-image.jpg'
---

# Basic React component with Typescript

```jsx
interface Props {
  name: string
  isDev: boolean
  favLanguage: string
}

const Dev: React.FC<Props> = ({ name, isDev, favLanguage }) => {
  return (
    <div>
      <h1>{name}</h1>
      {isDev && (
        <h4>Favorite programming language</h4>
        <span>{favLanguage}</span>
      )}
    </div>
  )
}

export default Dev

// Using component
<Dev name="Nico Pellerin" isDev={true} favLanguage="Rust" />
```
