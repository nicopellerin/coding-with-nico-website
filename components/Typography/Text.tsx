import styled from 'styled-components'

const Text = styled.p`
  font-size: 1.8rem;
  color: var(--textColor);
  font-family: 'Lora', sans-serif;
  line-height: 1.65;
  max-width: 90ch;
  margin-bottom: 24px;
`

export default Text
